#include "assembler.h"
#include <getopt.h>

using namespace std;

int main(int argc, char* argv[]) {
    if(argc != 2) {
        return 1;
    }

    //Assembler a(argv[1]);
    //a.Compile();
    //a.Write();
}
